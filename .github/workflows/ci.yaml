name: Run CI Tests
on:
  push:
jobs:
  CI:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Go
        uses: hashicorp/setup-golang@v1
        with:
          version-file: go.mod
      - name: Build
        run: go build ./...
      - name: Test
        run: go test -v -race ./...
